<%= form_for(@base_plate) do |f| %>
  <% if @base_plate.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@base_plate.errors.count, "error") %> prohibited this base_plate from being saved:</h2>

      <ul>
      <% @base_plate.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :height %><br>
    <%= f.text_field :height %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
